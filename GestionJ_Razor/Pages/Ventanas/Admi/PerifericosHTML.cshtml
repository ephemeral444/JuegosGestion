@* Pages/Ventanas/Admi/PerifericosHTML.cshtml *@
@page
@model GestionJ_Razor.Pages.Ventanas.Admi.PerifericosHTMLModel
@{
    ViewData["Title"] = "Perifericos";
}

<center>
    <div>
        <form method="POST" enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Perifericos</h3>
            </div>

            <div>
                <input type="submit" value="Refrescar" class="btn btn-secondary" asp-page-handler="BtRefrescar" />
                <input type="submit" value="Nuevo" class="btn btn-secondary" asp-page-handler="BtNuevo" disabled="@(Model.Periferico != null)" />
                <input type="submit" value="Guardar" class="btn btn-secondary" asp-page-handler="BtGuardar" disabled="@(Model.Periferico == null)" />
            </div>

            @if (Model.Periferico != null)
            {
                <input type="hidden" asp-for="@Model.Periferico!.Id" />
                <br />
                <div>
                    <div class="form-group">
                        <label>Video</label>
                        <div class="col-6">
                            <input type="checkbox" asp-for="@Model.Periferico!.Video" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Audio</label>
                        <div class="col-6">
                            <input type="checkbox" asp-for="@Model.Periferico!.Audio" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Teclado</label>
                        <div class="col-6">
                            <input type="checkbox" asp-for="@Model.Periferico!.Teclado" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Raton</label>
                        <div class="col-6">
                            <input type="checkbox" asp-for="@Model.Periferico!.Raton" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Mando</label>
                        <div class="col-6">
                            <input type="checkbox" asp-for="@Model.Periferico!.Mando" />
                        </div>
                    </div>
                </div>
            }

            @if (ViewData.ContainsKey("Mensaje"))
            {
                <div class="alert alert-danger mt-2">
                    @ViewData["Mensaje"]
                </div>
            }

            @if (Model.Periferico != null && Model.Borrando)
            {
                <div class="popup">
                    <div class="pp_ms_inner">
                        <div style="width:95%">
                            <h2 align="left">Mensaje</h2>
                            <h6 align="left">¿Desea borrar el Periferico?</h6>
                            <h6 align="left">Id: @Model.Periferico!.Id</h6>
                            <input type="submit" value="Cerrar" asp-page-handler="BtCerrar" class="btn btn-secondary" />
                            <input type="submit" value="Borrar" asp-page-handler="BtBorrar" class="btn btn-danger" />
                        </div>
                    </div>
                </div>
            }

            @if (Model.Lista != null && Model.Lista.Count > 0)
            {
                <br />
                <table class="table table-striped table-bordered table-hover">
                    <tr>
                        <th>Id</th>
                        <th>Video</th>
                        <th>Audio</th>
                        <th>Teclado</th>
                        <th>Raton</th>
                        <th>Mando</th>
                        <th>Acciones</th>
                    </tr>
                    @foreach (var elemento in Model.Lista)
                    {
                        <tr>
                            <td>@elemento.Id</td>
                            <td>@(elemento.Video ? "✔" : "✘")</td>
                            <td>@(elemento.Audio ? "✔" : "✘")</td>
                            <td>@(elemento.Teclado ? "✔" : "✘")</td>
                            <td>@(elemento.Raton ? "✔" : "✘")</td>
                            <td>@(elemento.Mando ? "✔" : "✘")</td>
                            <td>
                                <input type="submit" value="Modificar" asp-page-handler="BtModificar" class="btn btn-warning btn-sm" asp-route-data="@elemento.Id" />
                                <input type="submit" value="Borrar" asp-page-handler="BtBorrarVal" class="btn btn-danger btn-sm" asp-route-data="@elemento.Id" />
                            </td>
                        </tr>
                    }
                </table>
            }
        </form>
    </div>
</center>